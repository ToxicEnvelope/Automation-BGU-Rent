package il.co.bgurent.tests;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.WebDriver;
import org.testng.annotations.AfterClass;
import org.testng.annotations.BeforeClass;
import org.testng.annotations.Test;

import il.co.bgurent.utils.helper.UIMap;

public class BaseTest {

	
	static WebDriver driver;
	
	static final String CONF = "/src/il/co/bgurent/or/config.property";
	static final String DATA = "/src/il/co/bgurent/or/datafile.property";
	static final String LOC = "/src/il/co/bgurent/or/locator.property";
	

	@BeforeClass
	public void startUp() throws Exception {
		new UIMap(System.setProperty(("user.dir"),""));
		driver = UIMap.getDriver("CHROME");
		driver.get(UIMap.getData("url"));
		driver.manage().timeouts().implicitlyWait(7500, TimeUnit.MILLISECONDS);
	}
	
	@AfterClass
	public void tearDown() throws Exception {
		if(driver != null) {
			driver.manage().deleteAllCookies();
			driver.quit();
		} 
	}
	
	@Test
	public void test() {
		System.out.println("XXXXXXXXXXXXXXXXXXXXXXXXX");
	}
}
